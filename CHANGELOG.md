# Changelog

Все значимые изменения проекта фиксируются в этом файле.
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

---

## [Unreleased]

### Изменено
- Секреты (`SECRET_KEY`, `FIRST_ADMIN_*`) вынесены из `docker-compose.yml` в `.env`
  (через `env_file` с `required: false`); добавлен `.env.example`.

### Добавлено
- `deploy/nginx.conf` — пример reverse-proxy для деплоя на VPS (Nginx + Certbot/HTTPS).
- `docker-compose.prod.yml` — самодостаточная прод-конфигурация (порт привязан к
  `127.0.0.1`, наружу только через Nginx; `.env` обязателен).
- `DEPLOY.md` — пошаговая инструкция деплоя на VPS (Ubuntu) за Nginx + HTTPS.
- Внутренняя чистка после ревью: общий `_decode` для JWT, общий `ROLE_LABEL`,
  guard `require_admin` на уровне роутера `/users`, удаление мёртвого кода.

---

## [2.2.0] — 2026-06-03

### Добавлено
- **Множественные совещания** — сущность `Meeting` для разделения задач по совещаниям/проектам:
  - Поля совещания: тема, дата, заметки/протокол, участники (исполнители через M2M `meeting_participants`)
  - Задача опционально привязывается к одному совещанию (`meeting_id`, FK → `meetings`, `ON DELETE SET NULL`)
  - Backend: модель `Meeting`, миграция `0003_add_meetings` (idempotent), схемы/репозиторий/сервис/роутер
    `/api/meetings` (CRUD, поиск, пагинация), фильтр `GET /api/items?meeting_id=` (0 — задачи без совещания)
  - `ItemResponse` дополнен `meeting_id` и `meeting_title`
  - Frontend: страница `/meetings` (CRUD карточками, форма с `ExecutorMultiSelect` для участников),
    пункт меню «Совещания», селект совещания в форме задачи, фильтр «Совещание» на главной
  - Переход со страницы совещания «Задачи (N)» → главная с фильтром (`/?meeting=<id>`)
  - При удалении совещания привязка у задач сбрасывается (meeting_id → null)
- `ExecutorMultiSelect` получил проп `label` (переиспользуется для «Участники» в форме совещания)

---

## [2.1.2] — 2026-06-03

### Изменено
- Поле выбора исполнителей переделано в поисковый мультиселект (`ExecutorMultiSelect.svelte`) —
  удобно при большом числе инженеров и отделов:
  - строка поиска по имени исполнителя или названию отдела (подстрочное совпадение)
  - выбранные показаны чипами с кнопкой удаления прямо в поле + счётчик «выбрано N»
  - выпадающий список сгруппирован по отделам, отделы сворачиваются/разворачиваются
  - кнопка «Выбрать все / Снять все» для каждого отдела, счётчик выбранных в группе
  - «Очистить всё», закрытие по клику вне поля и по Esc
  - старый плоский список со всеми чекбоксами убран

### Исправлено
- Сворачивание отдела в мультиселекте больше не закрывает всю панель выбора: обнаружение клика
  вне поля переведено на `event.composedPath()` (устойчиво к удалению кликнутого узла при
  переключении иконки сворачивания)

---

## [2.1.1] — 2026-06-03

### Изменено
- В боковой панели задачи кнопки «Удалить» и «Сохранить» перенесены вниз — после блока истории статусов
  (раньше шли сразу за полями формы, до статусов). Кнопка «Сохранить» вынесена из `ItemForm`
  в `ItemDrawer` и связана с формой через HTML-атрибут `form="item-edit-form"` (нативная отправка
  и валидация `required` сохранены). В `Button` добавлен проп `form`.

---

## [2.1.0] — 2026-06-02

### Добавлено
- **Многопользовательская авторизация** на основе JWT (python-jose + passlib/bcrypt):
  - Три роли: `viewer` (только чтение), `editor` (полный CRUD), `admin` (editor + управление пользователями)
  - Access-токен (30 мин) + Refresh-токен (7 дней), хранятся в `localStorage`
  - Автоматическое обновление access-токена при 401 (с повтором запроса)
  - Принудительный logout при истечении refresh-токена (`auth:logout` событие)
- **Backend**: `app/security.py`, `app/dependencies.py`, `app/models/user.py`, `app/schemas/auth.py`, `app/schemas/user.py`, `app/repositories/user_repository.py`, `app/services/auth_service.py`
- **Роутеры**: `POST/GET /api/auth/*` (login, refresh, me), `GET/POST/PATCH/DELETE /api/users` (только Admin)
- **Миграция** `0002_add_users.py` — таблица `users` (idempotent)
- **Seed первого Admin** при первом запуске (из `FIRST_ADMIN_USERNAME`/`FIRST_ADMIN_PASSWORD`)
- **Frontend**: `auth.svelte.ts` (Runes store), страница `/login`, Admin-панель `/admin` (CRUD пользователей)
- Auth guard в `+layout.svelte`: неавторизованных перенаправляет на `/login`
- Блок пользователя в header: имя, роль, кнопка выхода; ссылка «Пользователи» только для Admin
- Защита всех существующих API-роутеров: GET → `get_current_user`, мутации → `require_editor`

### Изменено
- `api/client.ts`: добавлен `Authorization: Bearer` header в каждый запрос; обработка 401 с refresh-логикой
- `docker-compose.yml`: добавлены переменные `SECRET_KEY`, `FIRST_ADMIN_USERNAME`, `FIRST_ADMIN_PASSWORD`

---

## [2.0.1] — 2026-06-01

### Добавлено
- Быстрый просмотр истории статусов прямо из таблицы без открытия боковой панели:
  компонент `StatusPeek.svelte` — иконка с количеством статусов в колонке «Последний статус»,
  по клику открывается popover с полной историей (подгружается по требованию)

### Исправлено
- Окно истории статусов теперь следует за своей строкой при прокрутке (пересчёт позиции
  на scroll/resize, `capture:true` для вложенного скролла таблицы) + авто-флип вверх при нехватке места снизу

### Изменено
- Тёмная тема переведена на нейтральные серые тона (`#111827`/`#1f2937`) — убрана избыточная синяя насыщенность фона
- Чипы исполнителей вынесены в класс `.exec-chip` с читаемым контрастом в обеих темах
  (светлый текст на приглушённом фоне в тёмной теме вместо синего на тёмно-синем)
- Бейджи приоритета («Высокий»/«Средний»/«Низкий») приведены к одинаковой ширине (72px, выравнивание по центру)
- Выпадающие списки: нативная стрелка заменена кастомной, сдвинутой от правого края к центру (единый стиль для всех `select`)

---

## [2.0.0] — 2026-06-01

### Добавлено (Этапы 4–6: UX-полировка, системные фичи, Docker)
- **UX:** анимации Drawer/Modal/Toast/BulkActions (Svelte transitions), перенос фокуса в Drawer при открытии, Esc-закрытие, ARIA-метки
- **Печать:** стили `@media print` под Tailwind-разметку (классы `no-print`/`print-only`), печатный заголовок с датой
- **Системные фичи:** счётчик просрочки в заголовке вкладки, тёмная тема с предзагрузкой против FOUC и `prefers-color-scheme`, персист фильтров в localStorage, синхронизация открытой задачи с URL
- **Docker:** рабочий multi-stage `Dockerfile`, `docker-compose.yml` (prod, healthcheck через Python вместо curl), `Dockerfile.dev` + `docker-compose.dev.yml` (hot-reload), `.dockerignore`
- **Сидинг:** `scripts/seed.py` — наполнение тестовыми данными (4 отдела, 8 исполнителей, 18 задач, статусы, M2M-связи)
- **Документация:** `docs/ARCHITECTURE.md`, `docs/MIGRATION_GUIDE.md`, обновлён `README.md`, обновлён skill `run-protocol` (driver.sh: `/health`, PaginatedResponse)

### Исправлено (build-блокеры backend, найдены при запуске Docker)
- `pyproject.toml`: невалидный `build-backend` → `setuptools.build_meta`
- `Dockerfile`: установка пакета до копирования `app/` → копирование backend перед `pip install`
- Запуск Alembic: `python -m alembic` не работает → консольный скрипт `alembic` (+ `alembic.config` в lifespan)
- **Коллизия имён:** каталог миграций `backend/alembic/` с `__init__.py` затенял установленный пакет `alembic` → удалён `__init__.py`
- Начальная миграция сделана идемпотентной (создаёт только отсутствующие таблицы + добавляет недостающие столбцы) — корректный путь v1→v2

### Проверено (end-to-end через Docker)
- `docker compose build` + `up` → контейнер **Up (healthy)**
- `/health`, `/api/items` (PaginatedResponse), `/api/departments`, `/api/executors` — корректные v2-структуры с M2M-исполнителями и историей статусов
- CSV-экспорт с UTF-8 BOM, smoke-driver `driver.sh` — все проверки пройдены
- Фронтенд: `svelte-check` 0 ошибок/0 предупреждений, production build успешен

---

## [2.0.0-stage-3] — 2026-06-01

### Добавлено (Этап 3: Перенос бизнес-логики)
- **Таблица задач**: `ItemsTable` (сортируемые колонки, выбор всех, пагинация) + `ItemRow` (чекбокс, inline-селект состояния, чипы исполнителей, срок с расчётом, счётчик статусов)
- **Drawer задачи**: `ItemDrawer` — форма + история статусов, кнопка «открыть в новой вкладке», `{#key itemId}` для пересоздания формы
- **Формы**: `ItemForm` (все поля + мультивыбор исполнителей по отделам), `StatusForm` (дата + примечание)
- **Timeline**: список статусов с выделением последнего, добавление/удаление
- **Фильтры**: `Dashboard` (4 тайла-счётчика), `SearchBar` (debounce 150мс), `Filters` (отдел/исполнитель/приоритет + сброс)
- **Групповые операции**: `BulkActions` — плавающая sticky-панель, появляется при выделении, массовая смена состояния и удаление
- **Справочники**: `DepartmentsList` и `ExecutorsList` — добавление, inline-редактирование, удаление, группировка исполнителей по отделам, счётчики
- **Common**: `Pagination` (с многоточиями), `ConfirmDialog` (promise-based через confirmStore)
- **Сторы**: `confirm.svelte.ts` (promise-based подтверждения)
- Экспорт CSV/XLSX и импорт CSV из тулбара
- Базовая синхронизация открытой задачи с URL (`?task=N`)
- Оптимистичные обновления состояния задач с откатом при ошибке
- **Верификация:** svelte-check 0 ошибок/предупреждений, production build успешен

---

## [2.0.0-stage-2] — 2026-06-01

### Добавлено (Этап 2: Фронтенд-каркас)
- Svelte 5 + SvelteKit проект с adapter-static в SPA-режиме (fallback index.html → dist/)
- Tailwind CSS 3.4 с тёмной темой через `data-theme` (совместимо с v1)
- CSS-переменные тем Light/Dark портированы из v1 в `app.css`
- Анти-FOUC: тема применяется в `app.html` до отрисовки
- HTTP-клиент `lib/api/client.ts`: apiGet/Post/Patch/Put/Delete, класс ApiError, обработка [409]
- API-модули по сущностям: items, statuses, departments, executors, export
- TS-типы под v2-схемы + npm-скрипт `generate-types` (openapi-typescript)
- Svelte 5 Runes stores (`.svelte.ts`):
  - `items` — $state/$derived, фильтрация/сортировка, оптимистичные обновления с откатом
  - `filters` — персист в localStorage, сортировка
  - `selection`, `theme` (prefers-color-scheme), `refs`, `toast`
- Базовые UI-компоненты: Button, Input, Select, Modal, Drawer, Badge, Toast, Loader
- Drawer/Modal — нативная реализация на Svelte-транзишенах (Esc, клик по фону, a11y)
- Утилиты портированы из v1: date (fmtDate/dueInfo с рабочими днями), format (plural), constants
- Роуты: `+layout.svelte` (шапка, nav, тема, Toast, badge просрочки во вкладке), `+page.svelte` (дашборд+поиск+таблица), `refs/+page.svelte`
- SPA-режим через `+layout.ts` (ssr=false)
- Старый ванильный TS-фронтенд удалён
- **Верификация:** svelte-check 0 ошибок/предупреждений, production build успешен

---

## [2.0.0-stage-1] — 2026-06-01

### Добавлено (Этап 1: Бэкенд)
- Структура директорий `backend/`, `scripts/`, `docs/`
- `backend/pyproject.toml` с зависимостями: FastAPI 0.115+, SQLAlchemy 2.0 async, aiosqlite, Alembic, Pydantic v2, openpyxl
- `backend/alembic.ini` + `alembic/env.py` в async-режиме (run_async_migrations)
- `backend/app/config.py` — Pydantic Settings с DB_PATH, CORS origins
- `backend/app/database.py` — async engine, get_db dependency с commit/rollback
- SQLAlchemy 2.0 модели (mapped_column стиль): Department, Executor, Item, Status, item_executor_table
- M2M нормализация: `item_executors` таблица вместо `executors_json`
- Pydantic v2 схемы: ItemCreate/Update/Response, StatusCreate/Response, DepartmentCreate/Response, ExecutorCreate/Update/Response, PaginatedResponse[T], StateEnum, PriorityEnum
- `BaseRepository[T]` — generic CRUD (flush, не commit)
- Конкретные репозитории с selectinload для async lazy loading
- Сервисы: ItemService, StatusService, DepartmentService, ExecutorService, CsvService
- HTTP роутеры: /items (PATCH вместо PUT), /statuses, /departments, /executors, /export, /import
- `GET /health` endpoint
- Lifespan с автозапуском `alembic upgrade head`
- Начальная миграция Alembic с WAL-режимом и FK правилами
- `scripts/migrate_data.py` — перенос executors_json → item_executors
- Тесты: conftest.py (in-memory SQLite, ASGI client), test_departments_api.py, test_items_api.py
- `ruff.toml`, `.editorconfig`
- `Dockerfile` v2 (multi-stage: Node→Python, dist→static)
- `docker-compose.yml` с healthcheck
- `docker-compose.dev.yml` + `Dockerfile.dev` (hot-reload backend + Svelte dev server)

---

## [2.0.0] — В разработке (Этапы 2-6)

### Добавлено
- **Полный рефакторинг на современный стек.** Переписывание с нуля всей кодовой базы согласно техническому заданию v2.
- **Backend: FastAPI + SQLAlchemy ORM** вместо монолитного `app.py` с сырыми SQL-запросами.
  - Структурированная архитектура: `routers/` → `services/` → `repositories/` → `models/`.
  - Асинхронный режим через `asyncio + aiosqlite`.
  - Валидация через `Pydantic v2` с поддержкой сложной бизнес-логики.
  - Интерактивная документация Swagger UI на `/api/docs`.
- **Frontend: Svelte 5 + Tailwind CSS** вместо ванильного TypeScript.
  - Реактивность через Svelte Runes (`$state`, `$derived`) без необходимости внешнего state-менеджера.
  - Компонентная архитектура с Bits UI для headless UI-компонентов.
  - Минималистичный дизайн через Tailwind CSS с переменными для Light/Dark theme.
  - End-to-end типизация: автоматическая генерация TS-типов из OpenAPI.
- **Нормализация БД:** замена JSON-массива `executors_json` на классическую таблицу Many-to-Many `item_executors`.
  - Эффективный поиск задач по исполнителю на уровне SQL.
  - Корректное каскадное удаление.
- **Миграции БД через Alembic:** версионирование схемы БД вместо ненадёжных `PRAGMA table_info` + `ALTER TABLE`.
- **Drawer вместо inline-панели:** боковая панель задачи выезжает справа, не ломая верстку таблицы. Современный паттерн UX.
- **Floating sticky panel для bulk-операций:** появляется только при выделении задач (как в Gmail/Notion).
- **Безопасность:** полное исключение инлайн-обработчиков событий и конкатенации строк (рисков XSS).

### Изменено
- **UI переделан на Drawer/Slide-over Modal** вместо inline-расширяющейся панели.
- **Структура проекта:** `backend/` и `frontend/` — разные экосистемы с собственными конфигурациями и тестами.
- **API от OpenAPI/Swagger:** полная типизация и документация автоматически.
- **Bulk-операции** с UX из современных веб-приложений (sticky floating panel).

### Deprecated (удалено из v1)
- `app.py` монолит.
- Ванильный TypeScript с прямыми DOM-манипуляциями.
- JSON-хранение исполнителей в одной колонке.
- Статические миграции БД.

### Исправлено
- ❌ **XSS-уязвимости** через инлайн `onclick` в HTML-строках → Svelte автоматический санитайзинг.
- ❌ **Неэффективный поиск по исполнителю** через JSON → SQL-запросы с JOIN на нормализованной таблице.
- ❌ **Нестабильные миграции** → Alembic с версионированием.

---

## [1.7.0] — 2026-05-31

### Добавлено
- **Боковая панель задачи.** Клик по строке таблицы открывает фиксированную боковую панель
  слева (300px): поля задачи (тема, состояние, приоритет, тикет, срок, исполнители),
  полный таймлайн статусов с формой добавления, кнопки «Сохранить» / «Отмена» / «Удалить».
  Открыта максимум одна панель; `Esc` закрывает.
- **Открытие задачи в отдельном окне.** Кнопка «⧉ В новом окне» в шапке панели открывает
  задачу по адресу `?task=<ID>` — только карточка без таблицы. На бэкенде добавлен `GET /api/items/{id}`.
- **«+ Добавить»** теперь тоже открывает боковую панель (единая форма создания и редактирования).

### Изменено
- Удалены: модалки задачи и истории, inline-форма быстрого статуса, правка темы по двойному
  клику, аккордеон статусов в ячейке. Логика вынесена в новый модуль `detail.ts` (вместо `modals.ts`).
- Ячейка «Статусы» в таблице упрощена: последний статус + бейдж «ещё N».

### Исправлено
- **Счётчик просроченных в заголовке вкладки** не обновлялся при удалении задачи
  (`updateTabTitle` теперь вызывается из `updateDashboardCounts` — единая точка).
- **«Выбрать все»** в шапке таблицы не показывал панель массовых действий: обработчик
  перекрашивает строки напрямую, не вызывая полный `render()` (который в контексте
  `change`-события «съедал» обновление панели).

## [1.6.2] — 2026-05-31

### Изменено
- **Меню «Действия»**: кнопки «Экспорт CSV», «Экспорт Excel», «Импорт CSV» и «Печать»
  убраны из тулбара в выпадающее меню. Открывается кнопкой «Действия ▾»,
  закрывается кликом снаружи или `Esc`.

## [1.6.1] — 2026-05-31

### Добавлено
- **Экспорт в Excel** — новая кнопка «⬇ Excel» в тулбаре, эндпоинт `GET /api/export/xlsx`.
  Форматирование: заморозка шапки, автоширина столбцов, цветовая подсветка приоритетов
  (красный / жёлтый / зелёный) и состояний (синий / серый / зелёный), перенос текста в
  длинных ячейках. Зависимость: `openpyxl 3.1.5`.

## [1.6.0] — 2026-05-31

### Добавлено
- **Счётчик просроченных задач в заголовке вкладки** — если есть незакрытые задачи с истёкшим
  сроком, заголовок браузерной вкладки меняется на `(N) Протокол совещаний`. Обновляется при
  каждой перерисовке таблицы и загрузке данных.
- **Inline-редактирование темы задачи** — двойной клик по ячейке темы открывает поле ввода
  прямо в строке таблицы. `Enter` сохраняет (PUT `/api/items/{id}`), `Escape` отменяет.
  Оптимистичный UI: изменение отражается мгновенно, откат — при ошибке сети.

### Исправлено / улучшено
- BACKLOG.md: отмечены выполненные пп. 12 (тёмная тема) и 13 (bulk-операции); добавлены
  пп. 14–15 (горячие клавиши, экспорт в Excel) и раздел стратегических задач (пп. 16–19).

## [1.5.1] — 2026-05-31

### Изменено (тёмная тема)
- **Фон страницы** в тёмной теме осветлён с почти-чёрного `#0a0a12` до тёмно-синего `#11182f`.
- **Иерархия слоёв исправлена**: раньше header (`#0f0f1e`) был темнее фона страницы, что читалось перевёрнуто. Теперь слои идут от тёмного к светлому:
  - фон страницы `#11182f` — самый тёмный слой;
  - header `#1c2845` — «поднят» над фоном;
  - карточки / таблица / модалки `#232f4d` — самые светлые «поднятые» поверхности (было `#1a1a2e` / `#1e2433`);
  - hover строк и кнопок `#2c3a5e` (было `#262d45`).
- Светлая тема не затронута.

## [1.5.0] — 2026-05-31

### Изменено (рефакторинг фронтенда)
- **Vite + TypeScript**: фронтенд перенесён в `frontend/` и собирается через Vite 5 + TypeScript 5.
  Результат сборки (`static/`) не изменился с точки зрения работы приложения.
- **Разбивка JS на модули** (`src/`):
  - `types.ts` — интерфейсы `Item`, `Executor`, `Department`, `Status` и вспомогательные типы.
  - `utils.ts` — чистые функции (`fmtDate`, `esc`, `plural`, `dueInfo`) и константы (`STATE_LABEL`, `PRIORITY_CONFIG`…).
  - `api.ts` — все HTTP-вызовы к бэкенду с типизированными возвратами.
  - `state.ts` — единый объект `AppState`, `saveViewState`, `loadViewState`.
  - `theme.ts` — управление тёмной/светлой темой.
  - `toast.ts` — уведомления.
  - `filters.ts` — `applyFilters`, `applySort`, DOM-события фильтров.
  - `render.ts` — `render`, `renderRow`, `renderAccordion`, вспомогательные DOM-функции.
  - `loader.ts` — `loadAll` (оркестрация загрузки данных).
  - `modals.ts` — модалки задачи и истории, быстрый статус, смена состояния, удаление.
  - `bulk.ts` — bulk-операции (выбор строк, массовые изменения).
  - `refs.ts` — вкладка «Справочники» (CRUD отделов и исполнителей), `loadRefs`.
  - `confirm.ts` — диалог подтверждения.
  - `main.ts` — точка входа: инициализация, обработчики событий, экспозиция в `window`.
- **Разбивка CSS на 5 файлов** (`src/styles/`):
  `theme.css`, `base.css`, `components.css`, `table.css`, `print.css`.
- **Dockerfile**: multi-stage сборка — Node-стейдж собирает фронтенд, Python-стейдж запускает бэкенд.

## [1.4.3] — 2026-05-31

### Исправлено
- Все столбцы главной таблицы теперь обновляются в реальном времени без перезагрузки страницы:
  - **Статус (из модалки истории)**: добавлен `syncItemStatuses()` — при открытии/обновлении модалки сводка в `allItems` синхронизируется с полным списком с сервера, и вызывается `render()`. Охватывает добавление и удаление статусов.
  - **Состояние** (бейдж + цвет): добавлен `updateRowStateVisuals()` — точечно обновляет `sel.value`, `className` селекта и ячейку срока при вызове `changeState()`.
  - **Форма редактирования** (тема, тикет, приоритет, состояние, срок, исполнители, статус): заменён `loadAll()` на точечное обновление записи в `allItems` с последующим `render()`. ID исполнителей резолвятся в полные объекты из `allExecutors`. При создании новой задачи `loadAll()` сохранён для получения ID от сервера.

## [1.4.2] — 2026-05-31

### Исправлено
- `updateRowStateVisuals` теперь синхронизирует и выбранную опцию селекта (`sel.value`), а не только его цвет. Раньше при программной смене состояния (`changeState` не из самого дропдауна) отображаемая подпись рассинхронизировалась с реальным состоянием: селект мог показывать «Закрыто», тогда как цвет и сохранённое в БД состояние были «Отложено» — после перезагрузки подпись «возвращалась» к фактическому состоянию.

## [1.4.1] — 2026-05-31

### Исправлено
- При смене состояния задачи цвет селекта и ячейка срока теперь обновляются мгновенно (без полной перерисовки таблицы):
  - селект перекрашивается под новое состояние (в работе / отложено / закрыто);
  - срок пересчитывается через `dueInfo` — при переводе в «Закрыто» подпись просрочки исчезает, при возврате — снова появляется.
- Добавлен помощник `updateRowStateVisuals(item)` для точечного обновления визуала строки; используется и при откате после ошибки API.

## [1.4.0] — 2026-05-31

### Добавлено
- **Пагинация**: таблица задач разбита на страницы. Панель пагинации появляется автоматически, когда задач больше одной страницы, и скрывается при малом числе записей.
  - Выбор размера страницы: 10 / 20 / 50 / Все (сохраняется в localStorage).
  - Умные кнопки навигации: `‹ 1 … 4 5 6 … 12 ›` с многоточием при большом числе страниц.
  - Счётчик `1–20 из 43`.
  - Нумерация строк глобальная (не сбрасывается на каждой странице).
  - При смене фильтра, поиска или сортировки — сброс на страницу 1.
  - «Выбрать все» выделяет только текущую страницу, при переходе на другую — выделение сбрасывается.
  - Панель скрыта при печати.
  - Адаптивный вид на мобильных.

- **Улучшенный калькулятор просрочки**:
  - «Завтра» вместо «через 1 день».
  - Для просроченных задач (≥ 3 дней) показывает рабочие дни в скобках: «просрочено на 6 дней (5 рабочих)» — исключает субботу и воскресенье.
  - Относительное время для будущих дат: «через 2 нед.» (8–30 дней), «через 2 мес.» (> 30 дней).
  - Закрытые задачи по-прежнему не показывают срок.

## [1.3.2] — 2026-05-30

### Исправлено
- Кнопка переключения темы (🌙/☀️) прижималась к верху шапки. Добавлен `align-self: center` — теперь она выровнена по центру по вертикали, на одной линии со строкой поиска.

## [1.3.1] — 2026-05-30

### Исправлено
- **Читаемость тёмной темы**: в v1.2.0 на переменные были переведены только `header` и таблица, поэтому в тёмной теме множество элементов оставались светлыми (кнопки, выпадающие списки, дашборд-тайлы, справочники, модалки, формы), а текст статусов оказывался тёмным на тёмном фоне.
- Добавлен блок переопределений, действующий только при `data-theme="dark"`: кнопки `.btn-secondary`/`.btn-cancel`/`.btn-reset-adv`/`.btn-exec-remove`, `.toolbar-select`, дашборд-тайлы, чипы исполнителей, бейджи состояний, текст статусов, модалки и формы, справочники, история, форма добавления статуса, спиннер.
- Светлая тема не затронута — все правила изолированы под `:root[data-theme="dark"]`.
- Добавлен `color-scheme: dark` для корректного отображения нативных контролов (выбор даты, скроллбары).

## [1.3.0] — 2026-05-30

### Добавлено
- **Bulk-операции**: выделение нескольких задач и массовые действия (бэклог #13).
  - Колонка с checkbox в таблице; заголовок-чекбокс «Выбрать все видимые».
  - Синяя панель `bulk-bar` появляется при выделении ≥ 1 задачи.
  - **Массовая смена состояния**: выбрать новое состояние → «Применить» — все выделенные задачи обновляются оптимистично.
  - **Массовое удаление**: кнопка «🗑 Удалить» с диалогом подтверждения.
  - Escape снимает выделение (если нет открытых модалок).
  - Выделение сохраняется при перерисовке таблицы (фильтры, сортировка).
  - Чекбоксы и bulk-бар скрыты при печати.

### Изменено
- `removeRowFromTable` также удаляет связанную qs-строку.
- При одиночном удалении задача автоматически убирается из набора выбранных.

## [1.2.0] — 2026-05-30

### Добавлено
- **Тёмная тема**: кнопка toggle в шапке (иконка 🌙/☀️) для переключения между светлой и тёмной темой.
- **CSS-переменные для темизации**: все основные цвета вынесены в CSS-переменные (`--bg-primary`, `--text-primary`, `--accent` и т.д.).
- **Сохранение выбранной темы**: выбор темы сохраняется в localStorage и восстанавливается при загрузке.
- **Поддержка `prefers-color-scheme`**: при отсутствии сохранённой темы используется системное предпочтение.

### Изменено
- Header, таблица, фильтры и основные элементы теперь используют CSS-переменные вместо hardcoded цветов.
- Тёмная тема использует приглушённые цвета для комфорта при ночной работе.

## [1.1.0] — 2026-05-30

### Добавлено
- **Мобильный адаптив**: поддержка узких экранов (телефоны, планшеты).
  - На экранах до 768px таблица переходит в режим карточек (по одной задаче в строке).
  - На экранах до 480px интерфейс переходит в очень компактный режим: шапка сжимается, дашборд в 2 колонки, кнопки становятся меньше.
  - Справочники автоматически переходят на 1 колонку на мобильных.

### Изменено
- Таблица на мобильных: thead скрывается, каждая строка становится карточкой с горизонтальной разметкой.
- Дашборд на планшетах: переходит на 2 колонки вместо 4.
- Фильтры на мобильных: компактная разметка с возможностью переноса.
- Кнопки действий: адаптивные размеры для разных ширин экранов.

## [1.0.0] — 2026-05-30

### Добавлено
- **Оптимистичный UI**: основные операции (удаление задачи, смена состояния, добавление быстрого статуса) больше не вызывают полную перезагрузку `loadAll()`. Изменения применяются мгновенно в UI, затем синхронизируются с API.
- **Сохранение состояния DOM**: позиция прокрутки таблицы и раскрытые аккордеоны теперь сохраняются при операциях и восстанавливаются после обновления.
- **Откат при ошибках API**: если запрос к API упал, оптимистичное изменение откатывается и показывается сообщение об ошибке.

### Изменено
- Удаление задачи (функция `deleteItem`): вместо вызова `loadAll()` удаляет строку из таблицы, обновляет счётчики дашборда, отправляет DELETE запрос.
- Смена состояния (функция `changeState`): обновляет объект в памяти и счётчики без перерисовки всей таблицы.
- Добавление быстрого статуса (функция `saveQs`): добавляет статус в `recent_statuses` в памяти, перерисовывает только нужную строку.
- Удаление статуса из истории (функция `deleteStatus`): больше не вызывает `loadAll()`, только обновляет историю в модальном окне.

### Исправлено
- Потеря фокуса после любой операции с задачей (теперь состояние DOM восстанавливается)
- Потеря позиции прокрутки при добавлении/редактировании/удалении задач

## [0.9.2] — 2026-05-30

### Изменено
- Дашборд: тайл «Просрочено» (фильтр `overdue`) заменён на «Отложено» (фильтр `postponed`). Теперь все четыре тайла фильтруют по состояниям: Все / В работе / Отложено / Закрыто.
- Цвет тайла «Отложено» изменён с красного на жёлтый (`dash-tile--yellow`): границлева `#f59e0b`, активный фон `#fef3c7`.

## [0.9.1] — 2026-05-30

### Добавлено
- **Печать / @media print** (бэклог #10): кнопка «🖨 Печать» в панели действий вызывает `window.print()`. При печати: скрываются шапка, фильтры, кнопки действий и колонка редактирования; все аккордеоны статусов раскрываются; текст статусов не обрезается; сохраняются цвета приоритетов и шапки таблицы (`print-color-adjust: exact`); страница переносится по строкам, а не внутри них. Над таблицей выводится заголовок «Meeting Minutes» с текущим фильтром и датой печати.

## [0.9.0] — 2026-05-30

### Добавлено
- **Справочники — группировка исполнителей по отделу**: список исполнителей разбит на секции с заголовком отдела; «Без отдела» выводится последней группой.
- **Справочники — счётчик задач у исполнителя**: бейдж «N задач» / «нет задач» рядом с именем, вычисляется из уже загруженного `allItems` без доп. запросов к API.
- **Справочники — счётчик исполнителей у отдела**: аналогичный бейдж «N исполнителей» / «нет исполнителей» в карточке Отделов.
- **Справочники — поиск по исполнителям**: поле фильтра над списком; ищет по имени и названию отдела, не сбрасывает состояние редактирования.

## [0.8.4] — 2026-05-30

### Изменено
- Дашборд перенесён в строку фильтров: теперь тайлы «Все / В работе / Просрочено / Закрыто» находятся между фильтрами и кнопками действий, отцентрированы по экрану через `grid-template-columns: 1fr auto 1fr`. Отдельная строка дашборда удалена — интерфейс стал на одну полосу компактнее. Тайлы уменьшены (`min-width: 90px`, `padding: 8px 14px`).

## [0.8.3] — 2026-05-30

### Изменено
- Табы «Протокол» / «Справочники» переведены в стиль pill-chips: `align-items: center`, `border-radius: 6px`, активный таб — полупрозрачный белый фон (`rgba(255,255,255,.12)`), hover — более тёмный (`rgba(255,255,255,.07)`). Убран `border-bottom`-индикатор и растяжка на всю высоту шапки.

## [0.8.2] — 2026-05-30

### Исправлено
- CSS-правила `.logo` / `.logo-text` отсутствовали — добавлены; текст «Meeting Minutes» теперь полностью справа от SVG-иконки (flex-колонка)
- Иконка поиска заменена с цветного эмодзи 🔍 на минималистичный SVG, выровнен по центру вертикали
- Плейсхолдер поиска сокращён до «Поиск…»

## [0.8.1] — 2026-05-30

### Изменено
- Строка поиска перенесена в правый угол шапки (тёмная тема: полупрозрачный фон, белый текст)
- Кнопки «Экспорт», «Импорт», «+ Добавить» перемещены в строку фильтров и прижаты вправо через `margin-left: auto`
- Отдельный тулбар (`.toolbar`) упразднён — интерфейс сократился до трёх полос

## [0.8.0] — 2026-05-30

### Добавлено / Изменено
- **Логотип**: заголовок «Протокол совещания» заменён на SVG-иконку + «Meeting Minutes»
- **Дашборд как навигация**: тайлы «Все / В работе / Просрочено / Закрыто» стали кликабельными фильтрами; активный тайл подсвечивается цветом группы
- 4-й тайл — полное число закрытых задач (было «Закрыто за неделю»)
- Убрана отдельная строка кнопок-фильтров и кнопка «Только просроченные» — функции теперь в тайлах дашборда
- `currentFilter` принимает псевдо-значение `'overdue'`; переменная `filterOverdue` удалена

## [0.7.1] — 2026-05-30

### Добавлено
- **Запоминание вида** (бэклог #8): фильтр по состоянию, строка поиска, расширенные фильтры (отдел / исполнитель / приоритет / только просроченные) и сортировка сохраняются в `localStorage` (ключ `protocol_view_v1`) и восстанавливаются при перезагрузке страницы. Бэкенд не затронут.

## [0.7.0] — 2026-05-30

### Добавлено
- **Расширенные фильтры** (бэклог #7): под строкой состояний появилась панель с тремя выпадающими списками — отдел, исполнитель, приоритет — и кнопкой «Только просроченные». Фильтры накапливаются поверх поиска и фильтра по состоянию. При активном фильтре появляется кнопка «✕ Сбросить фильтры». Активные селекторы подсвечиваются фиолетовой рамкой. Списки отделов и исполнителей строятся динамически из справочников.

## [0.6.2.2] — 2026-05-30

### Изменено
- В раскрытом аккордеоне длинный текст статуса больше не обрезается, а переносится на следующие строки (верхняя строка с последним статусом по-прежнему в одну строку для компактности).

## [0.6.2.1] — 2026-05-30

### Изменено
- Кнопка раскрытия аккордеона статусов `▾` увеличена до размера выпадающего списка «Состояние» (крупнее стрелка и паддинги, вертикальное центрирование в строке).
- Ссылка «+ добавить / вся история» под статусами переименована в «+ добавить» (полная история доступна через аккордеон и ссылку «…и ещё N — вся история»).

## [0.6.2] — 2026-05-30

### Изменено
- **Статусы на главном экране — аккордеон.** Видна одна строка с последним статусом (текст в одну строку с обрезкой, полный — в тултипе). Кнопка `▾` разворачивает остальные статусы прямо в строке таблицы, без модального окна; повторный клик сворачивает. Если в подгруженные записи вошли не все статусы — внизу раскрытого блока ссылка «…и ещё N — вся история».

## [0.6.1] — 2026-05-30

### Изменено
- **Статусы на главном экране** теперь выводятся компактно: одна строка с последним статусом (дата + текст в одну строку с обрезкой по ширине, полный текст — в тултипе) и бейдж «+N» с числом скрытых записей. Это выравнивает высоту строк таблицы. Клик по «+N» (как и по ссылке «вся история») открывает полную историю.

### Добавлено
- API `GET /api/items`: поле `status_count` — общее число статусов задачи (используется счётчиком скрытых записей в аккордеоне). Доп. запрос к БД выполняется только когда статусов больше трёх.

## [0.6.0] — 2026-05-30

### Добавлено
- **Сводка-дашборд** над таблицей: три плитки со счётчиками — «В работе», «Просрочено», «Закрыто за неделю». Обновляются при каждой загрузке данных.
  - «Просрочено» — задачи не в состоянии «Закрыто», у которых срок истёк. Число красное при ненуле, серое при нуле.
  - «Закрыто за неделю» — закрытые задачи, у которых дата последнего статуса попадает в последние 7 дней (прокси для `closed_at`, которого нет в схеме).

## [0.5.1] — 2026-05-30

### Добавлено
- **Редактирование записей справочников.** У каждого отдела и исполнителя появилась кнопка ✏️ — открывает инлайн-редактирование прямо в строке: для отдела меняется название, для исполнителя — имя и отдел (выпадающий список). Сохранение по кнопке или `Enter`, отмена по `×` или `Escape`.
- API: `PUT /api/departments/{id}` и `PUT /api/executors/{id}`. Оба возвращают `409` при попытке занять имя, уже использованное другой записью; `404` — если запись не найдена.

## [0.5.0] — 2026-05-30

### Добавлено
- **Тосты** (всплывающие уведомления) вместо браузерных `alert()` — для успеха и ошибок операций (сохранение, удаление, импорт, дубли в справочниках).
- **Кастомный диалог подтверждения** вместо браузерного `confirm()` при удалении задач, статусов, отделов и исполнителей — в стиле приложения.
- **Обработка ошибок и состояний загрузки**: при загрузке таблицы показывается спиннер; при сбое сети — сообщение с кнопкой «Повторить». Все изменяющие запросы при ошибке показывают тост вместо тихого сбоя.
- **Относительная подпись срока**: «просрочено на N дней», «сегодня», «через N дней» (с правильным склонением). Срок «скоро истекает» (≤ 3 дней) подсвечивается жёлтым.
- **Закрытие модальных окон по `Escape`** (подтверждение → история → форма задачи).

### Изменено
- **Маркер приоритета** теперь содержит букву (В / С / Н) в дополнение к цвету — различим для дальтоников; добавлен `aria-label`.
- **Доступность**: кликабельные бейдж состояния и ссылка истории получили `role="button"`, фокус с клавиатуры и активацию по `Enter` / `Space`; добавлен видимый стиль фокуса.

## [0.4.2] — 2026-05-29

### Исправлено
- Исполнитель, удалённый из справочника, больше не теряется у задачи при её редактировании и сохранении. Бэкенд сохраняет привязку (ID), а в форме такой исполнитель показывается в группе «Удалён из справочника». Это соответствует обещанию «Задачи не затронуты» при удалении из справочника.

## [0.4.1] — 2026-05-29

### Добавлено
- `README.md` с документацией проекта: архитектура, модель данных, REST API, устройство фронтенда, миграции, разработка.
- `.gitignore` (исключает `data/`, артефакты Python и IDE).

## [0.4.0] — 2026-05-29

### Добавлено
- **Справочник отделов** — вкладка «Справочники» теперь содержит две секции: Отделы и Исполнители. Отделы добавляются/удаляются там же. При удалении отдела исполнители сохраняются, но теряют привязку.
- Поле **«Отдел»** у исполнителя — при добавлении в справочник можно выбрать отдел из списка.
- В форме задачи поле исполнителя заменено на **выпадающий список**, сгруппированный по отделам (`<optgroup>`). Записи без отдела — в группе «Без отдела».
- В таблице исполнители отображаются чипами с указанием отдела.
- Исполнители задачи хранятся по ID (а не по имени), миграция выполняется автоматически.
- Таблица `departments` в БД; колонка `department_id` в таблице `executors`.

## [0.3.1] — 2026-05-29

### Исправлено
- При редактировании задачи (например, смене приоритета) больше не создаётся пустая запись в истории статусов. Статус добавляется только если заполнено поле **описания**.

## [0.3.0] — 2026-05-29

### Добавлено
- Вкладка **«Исполнители»** — справочник исполнителей с добавлением и удалением. Добавление по Enter или кнопке.
- Динамические поля исполнителей в форме задачи: по умолчанию одна строка, кнопка «+ добавить исполнителя» добавляет ещё; кнопка «−» убирает (последнюю строку только очищает).
- Автодополнение в полях исполнителей из справочника (HTML5 datalist).
- Удаление исполнителя из справочника не затрагивает уже привязанные задачи.

### Изменено
- Исполнители задачи хранятся как JSON-массив в колонке `executors_json` вместо двух отдельных полей `executor`/`executor2`.
- API: поле `executors` теперь массив строк; в CSV исполнители разделяются символом `|`.
- Автоматическая миграция БД: данные из `executor` и `executor2` переносятся в `executors_json`.

## [0.2.0] — 2026-05-29

### Добавлено
- **Поиск** по теме, тикету и исполнителям — строка в тулбаре, фильтрует в реальном времени.
- **Сортировка** по столбцам: кликом по заголовку (тикет, тема, состояние, срок, исполнитель). Повторный клик меняет направление.
- **Приоритет** задачи (Высокий / Средний / Низкий) — поле в форме, цветная точка в таблице, сортировка по приоритету.
- **Inline-смена состояния** — клик по бейджу «В работе / Отложено / Закрыто» циклически переключает состояние без открытия модалки.
- **Быстрый статус** — кнопка `+` в строке задачи раскрывает инлайн-форму с полями даты и описания. Enter сохраняет, Escape закрывает.
- **Экспорт в CSV** — кнопка «⬇ Экспорт» скачивает все задачи (с последним статусом), UTF-8 BOM для корректного открытия в Excel.
- **Импорт из CSV** — кнопка «⬆ Импорт» загружает задачи из файла того же формата.
- Зависимость `python-multipart` для обработки загрузки файлов.
- Автоматическая миграция БД: добавление колонки `priority`.

## [0.1.5] — 2026-05-29

### Добавлено
- Необязательный столбец **«Номер тикета»** для задачи (поле в форме и колонка в таблице).
- Автоматическая миграция БД: добавление колонки `ticket`.

## [0.1.4] — 2026-05-29

### Исправлено
- Необязательные поля задачи (срок, исполнители) теперь можно **очистить**
  при редактировании: пустое значение сохраняется как `NULL`.
  Обязательные `topic` и `state` по-прежнему защищены от обнуления.

## [0.1.3] — 2026-05-29

### Добавлено
- Отображение **3 последних статусов** в строке задачи на главном экране
  (дата слева, описание справа).
- Поддержка **двух исполнителей** для одной задачи (поля «Исполнитель 1» и «Исполнитель 2»).
- Подробные комментарии в `app.py`, описывающие назначение функций и логику кода.
- Этот файл `CHANGELOG.md`.

### Изменено
- API `GET /api/items` теперь возвращает поле `recent_statuses` (до 3 статусов)
  вместо `last_status`.
- Автоматическая миграция БД: добавление колонки `executor2` в существующую таблицу.

## [0.1.2] — 2026-05-29

### Добавлено
- **История статусов** для каждой задачи: статусы вынесены в отдельную таблицу
  `statuses` (связь один-ко-многим с `items`).
- Панель истории статусов с возможностью добавлять и удалять отдельные статусы.
- API-эндпоинты:
  - `GET /api/items/{id}/statuses` — история статусов задачи;
  - `POST /api/items/{id}/statuses` — добавить статус;
  - `DELETE /api/statuses/{id}` — удалить статус.

### Изменено
- При создании/редактировании задачи можно одновременно добавить новый статус.

### Миграции
- Автоматический перенос статусов из старой схемы (поля в таблице `items`)
  в новую таблицу `statuses` при первом запуске.

## [0.1.1] — 2026-05-29

### Добавлено
- Первая версия приложения для ведения протокола совещаний.
- CRUD по задачам: тема, статус (дата + описание), состояние
  (в работе / отложено / закрыто), срок выполнения, исполнитель.
- Фильтрация задач по состоянию со счётчиками.
- Подсветка просроченных сроков.
- Self-hosted развёртывание через Docker / docker-compose.
- Хранилище SQLite с сохранением данных на хосте через volume.
