# CI для «Протокол совещаний».
# Запускается на push/PR в beta и main. Покрывает линт, тесты,
# проверку фронтенда и сборку Docker.
name: CI

on:
  push:
    branches: [beta, main]
  pull_request:
    branches: [beta, main]

# Отменять запуски для того же PR/ветки при новых коммитах
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Линт: ruff на бэкенде (фронтенд проверяется svelte-check в check-frontend) ──
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install backend
        working-directory: backend
        run: pip install -e ".[dev]"

      - name: Ruff (backend)
        working-directory: backend
        run: ruff check app/

      - name: Pyright (backend)
        working-directory: backend
        run: pyright app/

  # ── Backend: pytest с покрытием ────────────────────────────────────────────
  # Тесты используют in-memory SQLite (aiosqlite) — см. tests/conftest.py.
  # Production-стек (asyncpg) проверяется отдельным docker-build джобом.
  test-backend:
    name: Backend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install backend
        working-directory: backend
        run: pip install -e ".[dev]"

      - name: Run pytest
        working-directory: backend
        run: pytest -v

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-backend
          path: backend/.coverage
          retention-days: 14

  # ── Backend: pytest на реальном PostgreSQL ────────────────────────────────
  # Проверяет прод-семантику (ilike/регистр, типы, каскады), которой нет в SQLite.
  test-backend-postgres:
    name: Backend tests (PostgreSQL)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: protocol
          POSTGRES_PASSWORD: protocol
          POSTGRES_DB: protocol_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U protocol -d protocol_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install backend
        working-directory: backend
        run: pip install -e ".[dev]"

      - name: Run pytest (PostgreSQL)
        working-directory: backend
        env:
          TEST_DATABASE_URL: postgresql+asyncpg://protocol:protocol@localhost:5432/protocol_test
        run: pytest -v

  # ── Frontend: svelte-check + tsc ──────────────────────────────────────────
  check-frontend:
    name: Frontend type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend
        working-directory: frontend
        run: npm ci

      - name: svelte-check
        working-directory: frontend
        run: npm run check

      - name: Vitest
        working-directory: frontend
        run: npm run test

  # ── Docker: проверка production-сборки (только PR/push в main) ────────────
  docker-build:
    name: Docker build (prod)
    runs-on: ubuntu-latest
    needs: [lint, test-backend, check-frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build production image
        run: docker build -t protocol:ci-${{ github.sha }} .
